<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans KR', sans-serif;
            background-color: #3e3e3e;
            background-image: linear-gradient(rgba(255,255,255,0.05) 2px, transparent 2px), linear-gradient(90deg, rgba(255,255,255,0.05) 2px, transparent 2px);
            background-size: 50px 50px;
            padding: 0px 10px;
        }
        .bookshelf-container {
            max-width: 1200px;
            margin: 0 auto;
        }
        h1 {
            text-align: left;
            color: #e9e9e9;
            margin-bottom: 10px;
            font-size: 2.8em;
            font-weight: 600;
            letter-spacing: 2px;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.5);
        }
        .shelf {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: flex-end;
            gap: 40px 30px;
            padding: 30px 10px;
            border-bottom: 25px solid #6b4f3a;
            background: #8a7364;
            background-image: url('data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAGQAAABkCAYAAABw4pVUAAAAAXNSR0IArs4c6QAAAARnQU1BAACxjwv8YQUAAAAJcEhZcwAADsMAAA7DAcdvqGQAAABTSURBVHhe7cEBAQAAAIIg/69uSEABAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAIAnAwQYAAFGLS3pAAAAAElFTkSuQmCC');
            box-shadow: 0 10px 15px rgba(0,0,0,0.4), inset 0 2px 5px rgba(0,0,0,0.3);
        }
        .book-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
        }
        .book-cover {
            display: flex;
            flex-direction: column;
            justify-content: center; /* 텍스트를 중앙에 배치 */
            text-align: center;
            width: 190px;
            height: 260px;
            background-size: cover;
            background-position: center;
            color: white;
            text-decoration: none;
            padding: 20px;
            border-radius: 4px 6px 6px 4px;
            box-shadow: 5px 5px 15px rgba(0,0,0,0.4);
            transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            position: relative;
            overflow: hidden;
            border-left: 3px solid rgba(0,0,0,0.2);
        }
        .book-cover:hover {
            transform: translateY(-12px) scale(1.05) rotate(1deg);
            box-shadow: 12px 12px 30px rgba(0,0,0,0.5);
        }
        .book-cover::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to top, rgba(0,0,0,0.8) 0%, rgba(0,0,0,0.2) 50%, rgba(0,0,0,0.8) 100%);
        }
        .book-text-content {
            position: relative;
            z-index: 1;
        }

        /* ★★★ 변경된 부분: 책 제목/부제 스타일 수정 ★★★ */
        .book-title {
            font-size: 20px; /* 가장 큰 글씨 */
            font-weight: 600;
            line-height: 1.2;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
            margin-bottom: 15px;
            display: block; /* 위아래 마진 적용을 위해 */
        }
        .book-subtitle {
            font-size: 16px; /* 중간 크기 글씨 */
            font-weight: 600;
            opacity: 0.9;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.7);
            display: block;
        }

        .nameplate {
            background-color: #c5a987;
            color: #4a3c2e;
            font-size: 12px;
            font-weight: 600;
            padding: 4px 12px;
            border-radius: 2px;
            box-shadow: inset 0 0 5px rgba(0,0,0,0.2);
            border: 1px solid #a4876a;
            max-width: 180px;
            text-align: center;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }
    </style>
</head>
<body>
    <div class="bookshelf-container">
        <h1>Home</h1>
        <div class="shelf">
            <% if (storyList.length > 0) { %>
                <% storyList.forEach(story => { %>
                    <div class="book-item">
                        <a href="<%= story.url %>" class="book-cover" style="background-image: url('<%= story.coverImagePath %>');">
                            <div class="book-text-content">
                                <h2 class="book-title"><%= story.seriesTitle %></h2>
                                <span class="book-subtitle">Part <%= parseInt(story.partNumber) + 1 %>: <%= story.partTitle %></span>
                            </div>
                        </a>
                        <div class="nameplate"><%= story.genre %></div>
                    </div>
                <% }); %>
            <% } else { %>
                <p style="color: #4a4a4a;">표시할 스토리가 없습니다. `data` 폴더 구조를 확인해주세요.</p>
            <% } %>
        </div>
    </div>
</body>
</html>